---
// Hero section component for José Valverde's fitness coaching landing page
---

<section id="inicio" class="relative min-h-screen flex items-center pt-20 overflow-hidden">
  <!-- Background Image -->
  <div class="absolute inset-0 z-0">
    <img src="/foto_personal.webp" alt="José Valverde entrenador personal" class="w-full h-full object-cover" />
    <div class="absolute inset-0 bg-black/60"></div>
  </div>
  
  <!-- Content -->
  <div class="relative z-10 w-full py-20 md:py-28 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center max-w-4xl mx-auto">
      <div class="mb-6">
        <span class="inline-block bg-yellow-400/20 text-yellow-400 px-4 py-2 rounded-full text-sm font-semibold mb-4 border border-yellow-400/30">
          Entrenador Personal Certificado
        </span>
      </div>
      
      <h1 class="text-5xl md:text-6xl lg:text-7xl font-bold text-white mb-6 leading-tight">
        Transforma tu 
        <span class="bg-gradient-to-r from-yellow-400 to-yellow-500 bg-clip-text text-transparent">Cuerpo</span> y 
        <span class="bg-gradient-to-r from-yellow-400 to-yellow-500 bg-clip-text text-transparent">Mente</span>
      </h1>
      
      <p class="text-xl md:text-2xl text-gray-200 mb-12 leading-relaxed max-w-3xl mx-auto">
        Soy <strong class="text-yellow-400">José Valverde</strong>, tu entrenador personal especializado en 
        asesorías personalizadas. Te ayudo a alcanzar tus objetivos de fitness con 
        planes únicos diseñados específicamente para ti.
      </p>
      
      <div class="flex flex-col sm:flex-row gap-6 justify-center items-center">
        <button class="inline-flex items-center justify-center bg-yellow-400 hover:bg-yellow-500 active:bg-yellow-600 text-black font-semibold rounded-lg text-lg px-10 py-5 transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-yellow-400 focus:ring-offset-2 focus:ring-offset-black shadow-lg hover:shadow-xl transform hover:scale-105">
          Comenzar Ahora
        </button>
        <button class="inline-flex items-center justify-center bg-transparent border-2 border-yellow-400 text-yellow-400 hover:bg-yellow-400 hover:text-black active:bg-yellow-500 font-semibold rounded-lg text-lg px-10 py-5 transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-yellow-400 focus:ring-offset-2 focus:ring-offset-black backdrop-blur-sm hover:backdrop-blur-none">
          Consulta Gratuita
        </button>
      </div>
    </div>
    <!-- Scroll indicator -->
    <div class="text-center mt-16 flex justify-center">
      <div class="inline-flex flex-col items-center text-yellow-400/60 hover:text-yellow-400 transition-colors duration-300 cursor-pointer" onclick="document.getElementById('servicios')?.scrollIntoView({behavior: 'smooth'})">
        <span class="text-sm mb-2">Descubre más</span>
        <svg class="w-6 h-6 animate-bounce" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
        </svg>
      </div>
    </div>
  </div>
</section>

<script>
  // Add smooth scroll behavior for CTA buttons
  document.addEventListener('DOMContentLoaded', () => {
    const ctaButtons = document.querySelectorAll('.btn-primary, .btn-secondary');
    
    ctaButtons.forEach(button => {
      button.addEventListener('click', (e) => {
        if (button.textContent?.includes('Comenzar')) {
          e.preventDefault();
          document.getElementById('servicios')?.scrollIntoView({behavior: 'smooth'});
        } else if (button.textContent?.includes('Consulta')) {
          e.preventDefault();
          document.getElementById('contacto')?.scrollIntoView({behavior: 'smooth'});
        }
      });
    });
  });
</script>